# BikeChain — Backend Service (Phase 2)

This directory is prepared for **Phase 2: Backend Architecture**:
- Node.js & Express API server
- MongoDB connection and Mongoose models (`Motorcycle`, `User`, `ServiceRecord`, `TransactionAudit`)
- JWT authentication & role-based middleware
- RESTful routes for fast querying and metadata aggregation
- SHA-256 document hashing service
